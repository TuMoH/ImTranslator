<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SubtitleEditText" parent="Widget.AppCompat.EditText">
        <item name="android:textSize">@dimen/abc_text_size_body_1_material</item>
        <item name="android:background">@drawable/subtitle_edit_text_bcg</item>
    </style>

    <style name="LinkedAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textViewStyle">@style/LinkedTextView</item>
    </style>

    <style name="LinkedTextView" parent="android:Widget.TextView">
        <item name="android:autoLink">email</item>
    </style>

</resources>
